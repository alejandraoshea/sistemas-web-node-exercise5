<h1>Página Inicial</h1>

<ul>
  <% items.forEach(item => { %>
    <li><%= item %></li>
  <% }) %>
</ul>

<% if (!logged) { %>
  <a href="/login">Login</a>
<% } else { %>
  <a href="/private">Ir a la página privada</a>
  <br>
  <a href="/logout">Logout</a>
<% } %>
